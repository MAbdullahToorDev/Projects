# Toor Frontend

Static HTML/CSS/Vanilla JavaScript frontend.

Open `index.html` through a local static server. The frontend expects the backend API at `http://localhost:5000/api`.

No Dockerfile is included because Docker configuration is intentionally left for the project owner.
