# Toor Backend

Simple Node.js + Express + MongoDB API.

## Run

1. Copy `.env.example` to `.env`.
2. Set `MONGODB_URI`.
3. Install dependencies:
   `npm install`
4. Start:
   `npm start`

API: http://localhost:5000
Health: http://localhost:5000/api/health
